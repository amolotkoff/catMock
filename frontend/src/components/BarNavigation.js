import React, { useRef } from "react";
import Navigation from "./Navigation";

export default function BarNavigation() {
    return (<Navigation bars={[{icon: 'fa fa-home' } , {icon: 'fa fa-home'}]} />);
}