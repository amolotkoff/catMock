import React from 'react';
import { useEffect, useState } from "react";
import axios from "axios";
import Page from './Page';

export default function AppPage() {
  

    return (<Page>{<h1>sdfdsfdf</h1>}</Page>);
}




/*
 hello = () => {
  fetch('/api/time')
  .then( res => res.text())
  .then(res => { this.setState( {message: res})
  });
};*/